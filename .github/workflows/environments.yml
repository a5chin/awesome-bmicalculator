name: 'Environments'

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    environment:
      name: Deployment
      url: https://bmicalculator-a5chin.web.app
